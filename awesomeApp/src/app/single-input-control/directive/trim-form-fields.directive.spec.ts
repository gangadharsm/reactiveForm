import { TrimFormFieldsDirective } from './trim-form-fields.directive';

describe('TrimFormFieldsDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimFormFieldsDirective();
    expect(directive).toBeTruthy();
  });
});
